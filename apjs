
import { StyleSheet, Text, View , Button, ScrollView ,Image, ImageBackground, Pressable, Modal} from 'react-native'; 
import React from 'react';

const logoimg=require('./assets/adaptive-icon.png');
  const App= ({}) => { 
    const[isModalVisible,setIsModalVisible]=React.useState(false);
    return (
      <View style={styles.container}> 
      <ScrollView>
   {/* image ekleme  <Image
resizeMode='contain'
       style={{ width:"100%" ,
       height:200, 
       borderWidth:5, 
       borderColor:'red' }}
       source={require('./assets/dort.png')}/>

       <Image source={logoimg} style={{
        width:250,
        height:200,
        borderWidth:2,
        borderColor:'blue',
       }}/>

       <Image 
       style={{
        width:100,
        height:100,}}
       --url eklemek istersem-- source={{uri:'https://reactnative.dev/img/tiny_logo.png'}}
       /> */}
{/* yazılan metnin altına eklenen image <ImageBackground source={require('./assets/adaptive-icon.png')} 
 >
  <Text>ABCDFGHJHYUJHGF</Text>
</ImageBackground>

--geçişler ve modallar kullanımı:*/}
<Button 
title='Press'
onPress={() => setIsModalVisible(true)}
color={'midnightblue'}
/>
  <Modal visible={isModalVisible}
    onRequestClose={() => setIsModalVisible(false)}
    animationType='slide'
    presentationStyle='pageSheet' >
 
    <View style={{ flex:1, backgroundColor:'lightblue', padding:60 }}>
    <Text style={{ fontSize:30, fontWeight:'bold', marginBottom:20 }}
      >This is a Modal!</Text>
      <Button 
      title='Close'
      onPress={() => setIsModalVisible(false)}
      /> 
    </View>

    <View style={{ flex:1, backgroundColor:'blue', padding:60 }}>
    <Text style={{ fontSize:30, fontWeight:'bold', marginBottom:20 }}
      >This is </Text>
      <Button 
      title='Close'
      onPress={() => setIsModalVisible(false)}
      /> 
    </View>
    <View style={{ flex:1, backgroundColor:'purple', padding:60 }}>
    <Text style={{ fontSize:30, fontWeight:'bold', marginBottom:20 }}
      >This is a Modal!</Text>
      <Button 
      title='Close'
      onPress={() => setIsModalVisible(false)}
      /> 
    </View>

  </Modal>


<Pressable onPress={() => console.log('image pressed')}>
<Image source={logoimg} 
style={{ width:300,
  height:300, }}/>
</Pressable>

<Text> Paragraflar makalelerin yapı taşlarıdır. Birçok öğrenci paragrafları uzunluklarına göre tanımlar: bir paragraf en az beş cümleden oluşan bir gruptur, bir paragraf yarım sayfa uzunluğundadır, vb. Ancak gerçekte, cümleler arasındaki fikirlerin birliği ve tutarlılığı bir paragrafı oluşturan şeydir. Bir paragraf, "bir birim oluşturan bir cümle grubu veya tek bir cümle" olarak tanımlanır (Lunsford ve Connors 116). Uzunluk ve görünüm, bir makaledeki bir bölümün paragraf olup olmadığını belirlemez. Örneğin, bazı yazı stillerinde, özellikle gazetecilik stillerinde, bir paragraf yalnızca bir cümle uzunluğunda olabilir. Sonuç olarak, bir paragraf bir ana fikri destekleyen bir cümle veya cümle grubudur. Bu broşürde, buna "kontrol eden fikir" diyeceğiz çünkü paragrafın geri kalanında ne olacağını kontrol eder.</Text>
<Image source={logoimg} 
style={{ width:300,
  height:300,
  }}
/>
</ScrollView>   
    </View> ); 
       };
        const styles = StyleSheet.create({ 
 container: 
 { flex: 1,
  backgroundColor: 'plum',
  padding:60,
   },
  });

 export default App;